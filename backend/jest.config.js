module.exports = {
  testEnvironment: 'node',
  clearMocks: true,
  // You might add more configurations here later, like:
  // setupFilesAfterEnv: ['./tests/setup.js'], // For global test setup
  // coveragePathIgnorePatterns: ['/node_modules/'],
};
